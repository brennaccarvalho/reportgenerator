[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "reportgenerator"
version = "1.0.0"
description = "Streamlit report generator for spreadsheet-based analytics."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["app*"]
